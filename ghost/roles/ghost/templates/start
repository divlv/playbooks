#!/bin/bash
#
# Sample GHOST start script
#
cd {{ ghost_dir }} 
#
#docker build -t {{ blog_image }} . 

docker run -d -p {{ ghost_port }}:2368 -v {{ ghost_data_dir }}:/var/lib/ghost/content {{ blog_image }}

#